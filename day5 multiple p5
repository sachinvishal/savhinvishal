#include <stdio.h>

int main() {
    int multiplicand, n;
    scanf("%d %d", &multiplicand, &n);


    for (int i = 1; i <= multiplicand; i++) {
        printf("%d * %d = %d\n", i, n, i * n);
    }

    return 0;
}
